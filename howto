Sweetson's Work Portal 365 — Master AI Build Prompt
Create a complete, polished corporate web portal named "Sweetson's Work Portal 365".
The portal is a central dashboard for accessing frequently used work applications, websites, documents, and internal tools. It must have a professional corporate appearance, persistent link management, category-based navigation, search, settings, authentication, and a distinctive animated 3D logo.
1. Overall Design
Build a clean, premium corporate portal with:
Portal name: Sweetson's Work Portal 365
Professional corporate visual style
Primary visual palette:
Warm cream/off-white background
Navy/dark charcoal
Gold/brass accent
White content cards
Subtle borders and shadows
Responsive design for desktop, tablet, and mobile
Smooth hover and transition animations
Modern typography with an elegant serif heading combined with a clean sans-serif UI font
Avoid a generic template appearance
The interface should feel like an enterprise work dashboard
2. Header
Create a sticky top header containing:
Animated 3D portal logo
"Sweetson's Work Portal 365"
Small subtitle: "Corporate Workspace"
Logged-in username
Sign Out button
3D Logo
Create a genuine CSS 3D animated badge rather than a flat icon.
Requirements:
Use CSS `perspective`
Use `transform-style: preserve-3d`
Continuously rotate on the Y axis
Add a slight X-axis wobble/tumble
Front face displays:
S·365
Back face displays:
WORK
Front face should use a gold/brass gradient
Back face should use a dark navy gradient with gold lettering
Rounded corners
Subtle shadow
Add a subtle shine/premium effect if practical
Hovering over the logo pauses the animation
Use the same logo on:
Login screen
Main portal header
Example animation concept:
```css
transform-style: preserve-3d;
perspective: 500px;
animation: tumble 7s linear infinite;
```
The final result should look like a small rotating 3D corporate badge.
3. Authentication
The portal should open with a login screen.
Provide:
Username field
Password field
Sign In button
Create Account option
Create Account form
Username validation
Password validation
Invalid login error message
Sign Out functionality
Display logged-in username in the header
Passwords must not be stored as plain text.
For a standalone HTML prototype, hash the password before browser storage.
Clearly structure the authentication code so it can later be replaced with real backend authentication.
IMPORTANT:
A browser-only implementation is not real enterprise security. The application should be structured so that the localStorage authentication can later be replaced by a backend/database authentication system.
4. Main Navigation
After login, show a tab-based navigation system.
The main tabs should be:
All
Dynamic category tabs
⚙ Settings
Categories must be generated automatically from the links.
For example:
All
Tools
HR
Documents
Finance
Customs
Reports
Other categories entered by the administrator
Do not hard-code the category list.
If a new link uses a new category, automatically create a new category tab.
5. All / Link Dashboard
The All tab should display the available applications as a responsive card grid.
Each card should contain:
Automatically generated initials avatar
Application/link name
URL
Category badge
Remove button
The avatar should be generated from the first one or two letters of the application name.
Example:
"Merger 2.0" → "M2"
Cards should:
Have rounded corners
Have subtle shadows
Animate slightly upward on hover
Show the remove button on hover
Open the application in a new browser tab when clicked
6. Search
Add a search field near the top of the portal.
Search must filter links by:
Link name
URL
Category
Search should update results instantly while typing.
If there are no matching links, show a clean empty state.
7. Settings Tab
There must be a completely separate Settings tab.
IMPORTANT:
Add Link must NOT be a main navigation tab.
Instead:
Settings → Add Link
The Settings page must contain:
Add Link section
Fields:
Link Name
URL
Category
Buttons:
Add Link
Cancel
Validate:
Link name is required
URL is required
URL must be valid
Category can be an existing category or a new category
After successfully adding a link:
Save it persistently
Return to the Links view
Automatically select/filter to the newly created category
Show the newly added card immediately
8. Manage Links
Below Add Link in Settings, create a:
Manage Links
section.
Display every existing link in a management list.
Each row should show:
Initials/avatar
Link name
URL
Category
Added by
Remove button
Removing a link should:
Ask for confirmation
Remove the link
Persist the change
Refresh the interface immediately
9. Persistent Storage
For the standalone version, use browser localStorage.
Store:
Links
Users
Password hashes
Links must remain after closing and reopening the browser.
On first installation, preload this application:
Merger 2.0
URL:
https://microsoft-navision-365-data-merger-5-1b-pop8.onrender.com/
Category:
Tools
Added by:
System
Do not remove this initial link.
10. Link Data Structure
Use a structure similar to:
```javascript
{
    name: "Merger 2.0",
    url: "https://microsoft-navision-365-data-merger-5-1b-pop8.onrender.com/",
    category: "Tools",
    addedBy: "System"
}
```
Keep the data structure clean and easy to migrate to a backend database later.
11. User Attribution
When a user adds a link, store their username as:
```javascript
addedBy: currentUser
```
Show this information in the Settings → Manage Links section.
12. Responsive Design
The portal must work well on:
Desktop
Laptop
Tablet
Mobile
On smaller screens:
Header should adapt
Navigation tabs should remain usable
Cards should switch to fewer columns
Search should become full width
Settings forms should remain easy to use
Buttons should remain touch-friendly
13. UX Requirements
Use:
Smooth transitions
Clear hover states
Proper empty states
Clear validation messages
Confirmation before deleting
Consistent spacing
Professional visual hierarchy
Do not use excessive animations.
The 3D logo is the primary animated branding element.
14. Code Requirements
Provide a complete working application.
For the standalone prototype, create:
```text
Sweetsons_Work_Portal_365.html
```
The HTML file should contain:
HTML
CSS
JavaScript
so it can be opened directly in a browser without a build system.
Do not leave placeholders such as:
```text
TODO
implement later
...
```
All functionality described in this prompt must work.
15. Important Navigation Behavior
The navigation should work exactly as follows:
```text
All | Tools | HR | Documents | Other Categories | ⚙ Settings
```
Clicking:
All
Shows all links.
Category
Shows only links belonging to that category.
Settings
Shows:
```text
Add Link

Link Name
URL
Category

[Add Link] [Cancel]


Manage Links

Existing links...
```
The Add Link form must remain inside Settings.
Do not create a separate "+ Add Link" main navigation tab.
16. Visual Branding
The portal title must always be:
Sweetson's Work Portal 365
Use the exact apostrophe and capitalization.
The logo branding should use:
S·365
front face and:
WORK
back face.
Make the overall interface feel like a polished corporate internal portal rather than a basic HTML demo.
17. Future Backend Readiness
Organize JavaScript functions so that these can later be replaced by API calls:
Load users
Save users
Load links
Save links
Add link
Delete link
Login
Logout
Do not tightly couple the UI to localStorage.
Use clear functions such as:
```javascript
loadLinks()
saveLinks()
loadUsers()
saveUsers()
signIn()
signOut()
addLink()
removeLink()
render()
renderTabs()
renderSettings()
```
This will make it easier to migrate the application to:
Flask
Node.js
Supabase
Firebase
SQL database
Microsoft authentication / Entra ID
later.
18. Security Notice
Because this standalone version uses browser storage, do not claim that it provides enterprise-grade authentication.
The code should make it obvious that a production version should use:
Server-side authentication
Secure password handling
Database storage
HTTPS
Session management
Role-based access control
Admin permissions
19. Final Deliverable
Generate the complete working:
Sweetson's Work Portal 365
application as a single HTML file.
The finished portal must open directly in a browser and provide:
Login
Account creation
Animated 3D logo
Corporate header
All links
Dynamic category tabs
Search
Settings
Add Link inside Settings
Manage Links
Remove Links
Persistent browser storage
User attribution
Responsive design
Initial Merger 2.0 application
Make the final result polished, functional, visually consistent, and ready to use.
